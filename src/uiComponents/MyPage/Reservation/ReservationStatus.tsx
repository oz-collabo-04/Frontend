interface IReserveStatus {
  status: string;
}

const ReservationStatus = (status: IReserveStatus) => {
  // 예약 상태를 받아오는 API

  return <div>ReservationStatus</div>;
};

export default ReservationStatus;
